# cassandra-connector4s-scalatra-example #

## Build & Run ##

```sh
$ cd cassandra-connector4s-scalatra-example
$ sbt 
> test
> jetty:start
> browse
```

If `browse` doesn't launch your browser, manually open [http://localhost:8080/](http://localhost:8080/) in your browser.
